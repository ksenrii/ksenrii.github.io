# Blog 写作风格指南

这份指南用于让 AI 模仿我的博客写作方式。分析样本来自 `source/_posts` 中我自己写的文章，排除了 `TLS.md`、`Thread-Local-Storage.md`、`格密码基础.md`。

## 总体风格

我的博客偏向技术学习笔记和 CTF/密码学题解整理，不追求论文式严谨排版，也不追求公众号式铺垫。文章的核心目标是把一个概念、算法或攻击方法按自己的理解讲清楚，让读者能够顺着公式、例子和代码走一遍。

写作时应保持直接、朴素、偏口语化的技术说明风格。可以有少量解释性语句，例如“也就是说”“从形式上看”“换句话说”“注意到”“这说明”，这些句子用于把公式和直觉连接起来。

不要写得太像 AI。不要使用过于完整、工整、层层递进的长标题体系，也不要每一小节都写成“背景、原理、步骤、优缺点、总结”的固定模板。我的文章更像边学边整理的笔记，结构清楚，但不刻意包装。

## 标题习惯

标题层级要克制。

- 文章开头通常只有一个一级标题，直接写主题名，例如 `# RSA`、`# SM4`、`# 命令执行`。
- 主体多用二级标题 `##`，标题短而直接，例如 `## 概述`、`## 加密`、`## 解密`、`## 安全性`、`## 总结`。
- 如果文章是系列化知识点，可以使用编号标题，例如 `## 1. 背包问题`、`## 2. 基本加密思想`。
- 三级标题只在确实需要拆分某个二级标题时使用，例如 `### 3.1 为什么它容易解密`、`### 攻击示例`。
- 不要频繁使用四级标题。除非是 RSA 攻击这种长篇整理，才允许少量使用。
- 不要把标题写得很“正式”，例如少用“背景介绍”“核心原理分析”“实践应用总结”“进一步思考”这类 AI 常见标题。

标题应该像路标，不要承担过多解释。正文负责解释。

## 文章结构

常见结构如下：

1. 开头先给主题定义或来源，通常一到两段。
2. 插入 `<!-- more -->`。
3. 进入主体，按算法流程、概念关系或题目编号推进。
4. 需要数学表达时直接给公式，然后用自然语言解释公式含义。
5. 需要实践时给代码或 payload，并说明为什么这样写。
6. 最后可以有 `## 总结`，但总结要短，只提关键点。

密码学算法类文章常用结构：

```markdown
# 算法名

一句话介绍算法是什么，用在哪里，依赖什么困难问题。

<!-- more -->

## 概述

## 整体架构

## 明文处理

## 密钥处理

## 核心步骤

## 总结
```

攻击/题解类文章常用结构：

```markdown
# 主题

<!-- more -->

## 题目或攻击名称

先写特征。

再写攻击思路。

然后给公式或脚本。

最后说明如何得到结果。
```

Web 题解类文章可以直接按题号展开，例如 `## web29`、`## web30`，每节先贴图，再解释过滤条件，再给绕过方法和最终 payload。

## 语言特点

我的正文通常是“公式 + 解释 + 例子”的节奏。

常用表达：

- “也就是说，...”
- “从形式上看，...”
- “换句话说，...”
- “注意，...”
- “因此，...”
- “那么...”
- “这里的 ... 表示 ...”
- “可以理解为...”
- “它的核心思想是...”
- “其基本形式为...”
- “这说明...”

可以保留少量口语化表达，例如：

- “那先看看...”
- “直接...”
- “这里给出...”
- “剩下正常 ... 即可”
- “看起来...但...”

但不要过度口语化，不要写成聊天记录。

## 公式习惯

公式是文章的重要组成部分。能用公式表达的地方，应优先用公式把关系写清楚。

公式通常单独成块：

```markdown
$$
c \equiv m^e \pmod{N}
$$
```

公式后要用一两句话解释含义，不要只堆公式。

解释公式时可以直接说：

```markdown
也就是说，先对明文做 e 次幂，再对 N 取模。
```

对于算法流程，常用连续公式展示：

```markdown
$$
A = g^a \bmod p
$$

$$
B = g^b \bmod p
$$

$$
K = B^a = A^b = g^{ab} \bmod p
$$
```

不要为了显得正式而写大量定理环境、证明环境或复杂编号。

## 列表习惯

列表用于列条件、步骤、特征。列表要短，不能把文章写成 PPT。

常见用法：

```markdown
特征:

- 同一个模数 N
- 同一个明文 m
- 两个不同指数 e1, e2
```

步骤可以编号：

```markdown
1. 选取参数
2. 计算公钥
3. 计算密文
4. 使用私钥恢复明文
```

不要每个小点都写很长。长解释放到列表后面的正文里。

## 代码习惯

代码块要直接可用，偏向 Python、Sage、C/C++。代码通常用于验证算法、完成攻击或给出题解脚本。

代码前先说明用途：

```markdown
可以使用下面脚本求解：
```

代码后可以简单说明输出或关键逻辑，不需要逐行解释。

如果是 CTF 题解，允许保留待填参数：

```python
n =
e =
c =
```

代码注释可以有，但不要太密。注释应解释关键步骤，例如“求模逆”“处理负系数”“生成轮密钥”。

## 图片习惯

图片用于辅助理解，不做装饰。

图片语法通常很简单：

```markdown
![Diffie-Hellman](/images/DiffieHellman.png)
```

如果图片是题目截图，先放图，再解释代码逻辑或过滤条件。图片下方不需要写很长图注。

## 加粗习惯

加粗用于强调关键词或关键结论，不要滥用。

常见加粗对象：

- 算法名，例如 **AES**、**SM4**
- 关键性质，例如 **离散对数问题**
- 攻击条件，例如 **同一个模数 N**
- 重要结论，例如 **最后一轮不再执行 MixColumns**

不要整段加粗。不要把每个术语都加粗。

## 结尾习惯

结尾一般短一些，回到主题的关键理解。

好的结尾类似：

```markdown
因此，从整体上看，SM4 的加密过程可以概括为：明文分组、密钥扩展、32 轮轮函数迭代，以及最终的反序输出。
```

或者：

```markdown
因此，背包密码体制更适合作为密码学发展史中的重要实例来理解，而不是现代主流加密方案。
```

不要写鸡汤式结尾，不要写“希望本文对你有所帮助”。

## AI 写作时要避免

- 不要把一级、二级、三级标题排得过于繁琐。
- 不要每段都写得很完整、很像教材。
- 不要使用大量“首先、其次、最后、综上所述”的模板化连接。
- 不要给每个概念都加“背景”“意义”“应用场景”“优缺点”。
- 不要把所有内容都拆成小标题。
- 不要写太多抽象评价，重点放在公式、流程、例子、脚本。
- 不要为了显得严谨加入过多引用、脚注或论文腔。
- 不要在技术文章里写营销式开头。

## 推荐提示词

以后让 AI 写博客时，可以直接附上这段提示词：

```text
请按照我的博客风格写一篇 Hexo Markdown 文章。

风格要求：
1. 文章像技术学习笔记或 CTF 整理，不要写成论文或公众号。
2. 标题层级克制，只保留必要的一级、二级标题，三级标题只在确实需要时使用。
3. 开头直接介绍主题，然后放 `<!-- more -->`。
4. 正文按概念、公式、流程、例子、代码推进，不要写大量空泛背景。
5. 公式用 LaTeX 块展示，公式后用自然语言解释。
6. 代码块要实用，可以保留待填参数。
7. 语言保持直接、朴素、略口语化，可以使用“也就是说”“从形式上看”“换句话说”“注意到”“因此”等表达。
8. 不要使用 AI 常见的繁琐标题，例如“背景介绍”“核心原理分析”“实践应用总结”“进一步思考”。
9. 结尾短一点，只总结关键理解，不要写客套话。

请输出完整 Markdown。
```

## 简短版约束

如果只想快速约束 AI，可以使用：

```text
按我的博客风格写：标题少而直接，正文像技术笔记，先概念再公式再例子或脚本。不要论文腔，不要公众号腔，不要 AI 式多级标题。公式单独成块并解释，代码直接可用，结尾只收束关键点。
```
